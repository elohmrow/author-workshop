dialog: author-workshop:twoColumns
description: add components into two columns
renderType: freemarker
title: Two Columns
templateScript: /author-workshop/templates/components/twoColumns.ftl
areas:
  leftColumn:
    availableComponents:
      textImage:
        id: author-workshop:components/textImage
      twoColumns:
        id: author-workshop:components/twoColumns
  rightColumn:
    availableComponents:
      textImage:
        id: author-workshop:components/textImage
      twoColumns:
        id: author-workshop:components/twoColumns
parameters:
  divColLeft: col-sm-8
  divColRight: col-sm-4
